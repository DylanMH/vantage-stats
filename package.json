{
  "name": "vantage-stats",
  "version": "1.4.2",
  "main": "electron/main.js",
  "description": "Vantage Stats - Personal performance tracker for FPS Aim Trainers",
  "repository": {
    "type": "git",
    "url": "https://github.com/DylanMH/vantage-stats.git"
  },
  "license": "CC-BY-NC-SA-4.0",
  "scripts": {
    "start": "electron .",
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "dist:win": "npm run build && electron-builder -w",
    "dist:win:portable": "npm run build && electron-builder -w --win portable",
    "publish:win": "npm run build && electron-builder -w --publish always"
  },
  "dependencies": {
    "chokidar": "^4.0.3",
    "electron-log": "^5.4.3",
    "electron-updater": "^6.6.2",
    "express": "^5.1.0",
    "node-fetch": "^2.7.0",
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "@electron/rebuild": "^3.6.2",
    "electron": "^39.0.0",
    "electron-builder": "^26.0.12"
  },
  "build": {
    "appId": "com.vantagestats.app",
    "productName": "Vantage Stats",
    "directories": { "output": "dist" },


    "files": [
      "electron/**/*",
      "backend/**/*",
      "public/**/*",
      "frontend/dist/**/*",
      "assets/**/*",
      "package.json"
    ],


    "asar": true,
    "extraResources": [
      {
        "from": "assets",
        "to": "assets"
      }
    ],

    "win": {
      "target": [ "nsis" ],
      "artifactName": "Vantage-Stats-${version}-Setup.${ext}",
      "icon": "assets/vs-icon-logo.ico"
    },
    "nsis": {
      "oneClick": true,
      "perMachine": false,
      "allowToChangeInstallationDirectory": false
    },
    "publish": [
      { "provider": "github" }
    ]
  }
}
